# Base URL for the backend API.
# Default assumes backend is running locally on the same machine.
# For physical devices on WiFi, replace 'localhost' with your machine IP.
# Example: http://192.168.1.10:4444/api/v1
EXPO_PUBLIC_API_BASE_URL=http://localhost:4444/api/v1
